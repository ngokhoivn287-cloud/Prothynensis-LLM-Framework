# Mini3 MRTP Configuration

solver:
  count: 3
  parameter_count_per_model: 150000000
  hidden_dim: 256
  num_layers: 8
  num_heads: 4
  head_dim: 64
  ffn_hidden_dim: 1024
  activation: silu
  norm_type: rmsnorm
  vocab_size: 50304
  max_seq_len: 2048
  tie_embeddings: true
  dropout: 0.0

population_selection:
  redundancy_threshold: 0.95
  retention_ratio: 0.5
  quality_floor: 0.8

system:
  solver_params: 450000000
